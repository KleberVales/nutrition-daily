services:

  postgres:
    image: postgres:17
    container_name: nutrition-postgres
    environment:
      POSTGRES_DB: nutrition_daily
      POSTGRES_USER: nutrition_user
      POSTGRES_PASSWORD: nutrition_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
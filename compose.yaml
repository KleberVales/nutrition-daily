services:

  postgres:
    image: postgres:17
    container_name: nutrition-postgres
    environment:
      POSTGRES_DB: nutrition_dayli
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: